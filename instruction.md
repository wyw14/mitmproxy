同一个 flow 可能被复制、重放、导入或重新加载，用户需要知道这些实例是否来自同一条原始链路，但现有数据没有统一且可选的关联信息。请增加可选的 flow lineage 元数据：原始 flow 获得稳定的根标识，copy 产生子关系，client replay 保留根关系并区分每次尝试，导入重复内容不能错误复用其他 flow 的标识。FlowReader/Writer、HAR 交换、mitmweb JSON/API、事件通知和保存/加载都应在启用时保持关联信息一致；旧格式读取时生成隔离且可追踪的标识，取消或失败的 replay 不得留下半条关系。默认关闭时现有 flow 序列化、copy、load 和 replay 行为保持兼容。请覆盖跨入口查看、并发 replay、重复导入、旧文件和失败清理。
